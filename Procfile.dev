# Dev process tree: Postgres in docker, uvicorn on the host.
# Run with `mise run dev` (which calls `honcho start -f Procfile.dev`).
# Ctrl-C tears both processes down.
db: docker compose up db
web: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
